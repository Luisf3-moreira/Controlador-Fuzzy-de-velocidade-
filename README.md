# Controlador-Fuzzy-de-velocidade-
Rotina implementada em python para o controle de velocidade de um motor DC brushless (BLDC) usando fuzzy logic. O sistema de controle substitui a utilização do acelerador manual para variar a velocidade do motor. O código simula o controle usando fuzzy logic e usando o sistema PID
